<?xml version='1.0' ?>
<BuildGraph xmlns="http://www.epicgames.com/BuildGraph" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.epicgames.com/BuildGraph ../../Schema.xsd" >

    <!-- Target and configuration options -->
    <Option Name="TargetName" DefaultValue="" Restrict="" Description="If your project supports both Game and Client you can set this, otherwise one will be selected"/>
    <Option Name="EditorTargetName" DefaultValue="" Restrict="" Description="Specify the editor target name if different than (ProjectName)Editor"/>

    <Option Name="EditorPlatforms" Restrict="[^ ]+" DefaultValue="Win64" Description="Editor platforms to build (Separated by +)" />
    <Option Name="TargetPlatforms" Restrict="[^ ]*" DefaultValue="Win64+Switch" Description="Target platforms to build (Separated by +)" />
    <Option Name="TargetConfigurations" DefaultValue="Development" Description="Configuration(s) to build (Separated by +)"/>
    <Option Name="TestConfigurations" DefaultValue="$(TargetConfigurations)" Description="Configurations to package (Separated by +)"/>

</BuildGraph>