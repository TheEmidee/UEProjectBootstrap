"""
Each module in this package defines one top-level `Command` subclass
(and optionally sub-command classes next to it). They are picked up
automatically by `ue_bootstrap.discovery.discover_command_classes`.
"""
